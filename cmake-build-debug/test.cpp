#include <fstream>
int main(){
    string filename;
    string type;
    cout << "Please enter the file path;"
    cin >> filename;
    ifstream inputfile(filename);
    ifstream inputfile(filename);

    if (!inputfile.is_open()){
        cer<<"File can't be open" << endl;
        return 1;
    }

    if (!inputfile.is_open()){

    }

    if(!inputfile.is_open()){

    }

    if(!inputfile.is_open()){
        cer<<"Output file can't be open"<<endl;
        return 1;
    }

    for(int i = 0; i < num; ++i){

    }

    while(inputfile >> type ){

    }

    for()



}